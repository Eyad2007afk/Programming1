#include <stdio.h>
#include <string.h>

size_t read_line(char *buf, size_t cap, int *truncated){
    int i = 0;
    while (i < cap -1){
        c = getchar();
        if (c == '\n' || c == EOF){
            break;
        }
    }
}
int main(void){
    printf("Write a sentence : \n");
    char fub[5];
    
}